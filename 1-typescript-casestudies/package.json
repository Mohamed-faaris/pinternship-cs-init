{
  "name": "1-typescript-casestudies",
  "private": true,
  "scripts": {
    "tsr": "timeout -k 1s 1s sh -c 'bun x tsc --noEmit && bun run \"$1\"' --"
  },
  "devDependencies": {
    "typescript": "^5",
    "@types/bun": "latest"
  },
  "dependencies": {
    "reflect-metadata": "^0.2.2",
    "typedi": "^0.10.0"
  }
}